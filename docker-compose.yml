services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/bot.sqlite
    restart: unless-stopped
    volumes:
      - ./data:/app/data
